import 'package:flutter/material.dart';

class ItemofoffersPage extends StatelessWidget {
  const ItemofoffersPage({super.key});

  @override
  Widget build(BuildContext context) {
    return ListView(
      children: [
        
      ],
    );
  }
}